# DOS Launch Smoke Test

Goal: create the smallest DOS executable that TLAUNCH can run after CONTROL.TAT validation.

Suggested behavior:

```text
set a known graphics mode
draw a visible pattern
wait using polling
exit cleanly
```

Use VISENV first to verify the media layout and CONTROL.TAT path.
