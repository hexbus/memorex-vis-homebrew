# VISENV GBIOS Call Demo

This demonstrates the intended Python-side call pattern for initial GBIOS service stubs.

Future DOS examples should call the real interrupt path once contracts are byte-verified.
